<script>
	element = document.getElementById("modifyBtn");
element.addEventListener("click", myFunction);

function templateElement(element_name, text, parent) {
const template = document.createElement(element_name);
template.textContent = text;
parent.appendChild(template);
return template;
}

function myfunction() {
let form = templateElement("form", "", document.body);
let myLabel = templateElement('label', "Etes vous sur de vouloir supprimer cette article?", form);
let buttonOk = templateElement("button", "Ok", form, {id: "buttonOk"});
let buttonAnnuler = templateElement("button", "Annuler", form, {id: "buttonAnnuler"});

}


buttonOk = document.getElementById("buttonOk")
</script>
